# FanbaseHQ Scraper - Production Environment Variables
#
# IMPORTANT: Copy this file to config/.env (not deployment/.env)
# The scraper reads settings from config/.env via config/settings.py
#
# Command: cp deployment/.env.production.example config/.env

# ===================================
# API Keys (REQUIRED)
# ===================================

# OpenAI API Key - Get from https://platform.openai.com/api-keys
# Used in: config/settings.py -> OPENAI_API_KEY
OPENAI_API_KEY=sk-your-openai-api-key-here

# TwitterAPI.io Key - Get from https://twitterapi.io/dashboard
# Used in: config/settings.py -> TWITTER_API_KEY
TWITTER_API_KEY=your-twitterapi-key-here

# ===================================
# Email Configuration (REQUIRED for email delivery)
# ===================================

# SendGrid HTTP API Configuration (RECOMMENDED)
# Uses HTTPS (port 443) - never blocked by cloud providers
# More reliable and faster than SMTP
#
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# Create a new key with "Mail Send" permission (Full Access)
# Used in: config/settings.py -> SENDGRID_API_KEY, SENDGRID_FROM_EMAIL
SENDGRID_API_KEY=SG.your-sendgrid-api-key-here
SENDGRID_FROM_EMAIL=your-verified-sender@yourdomain.com

# Email recipient for results and alerts
# Used in: config/settings.py -> NOTIFICATION_EMAIL
NOTIFICATION_EMAIL=recipient@example.com

# ===================================
# Monitoring Configuration (OPTIONAL)
# ===================================

# Enable/disable email delivery (true/false)
# Used in: config/settings.py -> ENABLE_EMAIL_DELIVERY
ENABLE_EMAIL_DELIVERY=true

# Enable/disable error alert emails (true/false)
# Used in: config/settings.py -> ENABLE_ERROR_ALERTS
ENABLE_ERROR_ALERTS=true

# Number of consecutive days with no results before alert
# Used in: config/settings.py -> HEALTH_CHECK_DAYS
HEALTH_CHECK_DAYS=3

# ===================================
# Notes
# ===================================

# 1. File Location: This file should be copied to config/.env (NOT deployment/.env)
#    - config/settings.py loads environment variables from config/.env
#    - The .env file in config/ is gitignored for security

# 2. Gmail Users: You MUST use an app-specific password, not your regular Gmail password
#    - Enable 2FA on your Google account
#    - Generate app password: https://myaccount.google.com/apppasswords
#    - Use that password as SMTP_PASSWORD

# 3. Rate Limits:
#    - OpenAI: Pay-as-you-go (monitor usage at platform.openai.com)
#    - TwitterAPI.io: Free tier = 1 request per 5 seconds

# 4. Security:
#    - NEVER commit config/.env to git
#    - Keep your API keys secure
#    - Rotate keys if compromised
